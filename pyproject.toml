[build-system]
requires = ["flit_core >=3.2,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "Sportyfin"
authors = [{name = "Axel Mierczuk", email = "axelmierczuk@gmail.com"}]
dynamic = ["version", "description"]
